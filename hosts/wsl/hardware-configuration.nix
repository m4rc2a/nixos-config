# This is a placeholder - replace with actual hardware config from:
# sudo nixos-generate-config --show-hardware-config
{...}: {
  # WSL doesn't need traditional hardware config
  # The nixos-wsl module handles most of this
}
